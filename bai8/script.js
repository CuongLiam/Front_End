let a = Number(prompt("type a"));
let b = Number(prompt("type b"));
let c = Number(prompt("type c"));

alert(`so lon nhat torng 3 so la : ${Math.max(a,b,c)}`);
console.log(`so lon nhat torng ba so la : ${Math.max(a,b,c)}`);